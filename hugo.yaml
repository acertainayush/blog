baseURL: "http://www.slightlyconfused.org/"
title: "acertainayush"
languageCode: "en-us"
theme: "PaperMod"

markup:
  goldmark:
    extensions:
      passthrough:
        enable: true
        delimiters:
          block:
            - ["\\[", "\\]"]
            - ["$$", "$$"]
          inline:
            - ["\\(", "\\)"]
params:
  math: true
